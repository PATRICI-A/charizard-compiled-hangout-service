package com.charizard.compiled.hangout_service.domain.model.enums;

/**
 * Tipo de acceso a un parche.
 * <ul>
 *   <li>{@link #PUBLIC} — cualquier estudiante puede unirse sin invitación</li>
 *   <li>{@link #PRIVATE} — solo por invitación del capitán</li>
 * </ul>
 */
public enum ParcheType {
    PUBLIC,
    PRIVATE
}
